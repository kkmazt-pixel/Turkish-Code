"""turkish.code Çekirdek — the Python AI-brain sidecar (doc 09).

This package hosts all AI/business logic as an offline-installable sidecar that
speaks the Core Channel over stdio (doc 10). It performs no user-world side
effects directly; those are brokered by the Kabuk (doc 09 §9).
"""

__version__ = "0.0.0"
